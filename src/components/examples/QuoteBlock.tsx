import QuoteBlock from '../QuoteBlock'

export default function QuoteBlockExample() {
  return (
    <QuoteBlock
      quote="Every story is a doorway to another world, waiting for the reader brave enough to turn the handle."
      attribution="Night Phlox"
    />
  )
}
